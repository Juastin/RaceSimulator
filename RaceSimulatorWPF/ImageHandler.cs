using System;
using System.Collections.Generic;
using System.Drawing;
using System.Text;

namespace RaceSimulatorWPF
{
    public static class ImageHandler
    {
        private static Dictionary<string, Bitmap> imageBitmaps;

        //public string getImage(Section section)
        //{
        //    if (section == null)
        //        return new SectionData();
        //    if (positions.TryGetValue(section, out SectionData sectionData))
        //        return sectionData;
        //    sectionData = new SectionData();
        //    positions.Add(section, sectionData);
        //    return sectionData;
        //}
    }
}
